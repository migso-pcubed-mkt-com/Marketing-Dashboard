# DECISIONS.md — Marketing Dashboard

> Append-only decisions log. Newest entries at top.
> Do NOT duplicate content already present in CLAUDE.md current state.
> Format: one row per decision — date | decision | context (one sentence max).

---

## Maintenance Rules (for Claude Code)

- **Append only** — never edit or delete existing rows.
- **Newest first** — insert new rows at the top of the table.
- **One row per decision** — if multiple small decisions were made in one session, group them as one row with a compound decision label if needed.
- **Decision column** = what changed (action verb: "Add", "Fix", "Replace", "Remove", etc.).
- **Context column** = why, in one sentence. No narrative, no bullet lists.
- If a decision **supersedes** a previous one, note it: `(supersedes 2026-02-XX)`.
- Do not add rows for trivial implementation details — only decisions that affect architecture, patterns, cross-cutting concerns, or recurring bugs.

---

## Log

| Date | Decision | Context |
|------|----------|---------|
| 2026-06-29 | Excel import: detect theme-based fills (recovered + adapted from abandoned branch commit 143ea6b) — `THEME-<n>` sentinel for Microsoft accent themes 2-9 + `findRowBackgroundFill` to skip uniform row-background styling | Real user templates colour cells via Office theme+tint, not ARGB, so `fill.fgColor.argb` was undefined and those task signals were silently missed on import. The row-background guard prevents whole-row theme styling from turning a category row into a 12-task action. The other 3 commits on that branch (undo/redo, PPT Kanban, PPT Timeline) were already superseded on main/QA branch and abandoned |
| 2026-06-29 | Merge hardening from adversarial review: (1) merge no longer prunes tombstones (GC = load/save only) so a peer clock can't resurrect a deletion; (2) merge drops orphan task/action whose parent was concurrently deleted; (3) board-fallback keeps the tombstone guard so deleting the only board can't resurrect it; (4) conflict merge hoisted out of the `setBoardData` updater; (5) `contentSignature` try/catch | Two adversarial review agents on the cumulative diff found a board-resurrection edge, a concurrent-delete orphan, a clock-dependent GC-during-merge resurrection, and a side-effect-in-updater anti-pattern. All fixed with regression tests |
| 2026-06-29 | Add view integration smoke harness (`views.integration.test.jsx`) + fix missing `Icon.BarChart` (Dashboard crashed on an empty board) | The DashboardView empty-state rendered `<Icon.BarChart/>` which didn't exist → "Element type is invalid" crash on any board with zero tasks. The harness (render each view with realistic seed + empty data, assert no throw) caught it immediately — exactly the class of bug live recettage had to find before |
| 2026-06-29 | Trello auth reconnect-retry (backoff 2/5/15/30/60s on transient) + persistent degraded-save banner (`cloudSaveDegraded`) when neither Supabase nor GitHub accepts a save | A transient startup blip used to strand a valid user as guest-capability until reload; and a cloud-save failure only flashed a toast, so users couldn't tell their data was local-only. Retry reconnects on recovery; the banner stays until a cloud save succeeds |
| 2026-06-29 | Collaborator presence — dedicated Supabase `board_presence` channel + `lib/presence.js` + `PresenceIndicator` avatar row in the toolbar | BroadcastChannel only saw same-browser tabs; teammates editing concurrently were invisible. Presence is best-effort (try/catch, gated on useSupabase) so guest/offline degrades to no avatars. Kept off the `app_data_changes` channel because that effect re-subscribes on many deps |
| 2026-06-29 | Explicit conflict notification — `mergeBoardsEntityLevelWithMeta` reports when an incoming teammate edit discards a different-content local version; App surfaces `⚠️ "<name>" was changed by a teammate` instead of the silent `✅ Synced` on the Realtime / OCC / GitHub merge paths | Collaborative merges previously applied teammate overwrites of the same item silently; the user had no signal their on-screen value changed. Only flags meaningful content differences (volatile fields like updatedAt ignored) and only when the local side loses, to avoid noise from echoes and local-wins |
| 2026-06-29 | Add deletion tombstones (M18) — `lib/tombstones.js` records `{id,type,deletedAt}` on `board.deletions` + envelope `boardDeletions`; entity & board merges drop resurrected entities via `del >= updatedAt`; 30-day TTL GC | The entity-level collaborative merge re-added any entity present locally but absent from a peer's copy, so one user's deletion was silently undone by another user's stale state. Tombstones make delete vs edit a real last-write-wins. Undo works for free because `deletions` rides in the history snapshot; the Trello sync path is intentionally excluded (Trello is source-of-truth, self-heals) |
| 2026-06-28 | Fix Escape-to-close discarding edits — App's global `document` Escape listener no longer nulls `selectedTask`/`selectedAction`; the detail modals own Escape→save→close via their `window` listener | Live recettage found that pressing Escape after editing a field while focus was outside an input silently dropped the edit: App's `document` listener fires before the modal's `window` listener, so closing from App unmounted the modal and tore down its save-on-close listener before it ran. App's INPUT/TEXTAREA early-return had masked it until a blur occurred |
| 2026-05-12 | Typed history labels — `handleUpdateTask` / `handleUpdateAction` derive the label suffix from a real diff against the current entity (`description edited`, `schedule changed`, `reordered`, `moved`, etc.). No-op updates skip both `pushState` and the success notification | Users reported that drag + description edit collapsed into a single undo step because both pushed `Task "X" updated`. Typed suffixes prevent unrelated edits from coalescing while still letting the same gesture (e.g. resize mousemoves) keep one entry per 400 ms window. Diff (not key-based) is needed because TaskDetailModal sends the full form on close |
| 2026-05-12 | TimelineView drag DOWN now does a true swap — displaced occupants take the dragged task's previous lane instead of being pushed +1 | Pre-fix, dragging a task DOWN onto another created an empty lane between them. Detect direction (`dragStartLane < targetLane`) and swap. Upward drags + cross-action drops keep the original +1 push so existing UX is unchanged |
| 2026-05-12 | PPT Timeline task bars: title baked into the shape (`addShape` `text:` option), always black, `wrap: false` so long titles overflow horizontally instead of stacking lines | User wanted black-on-colour text that stays readable across themed slides; embedded text reduces shape count to 1 per task and makes the slide easier to edit in PowerPoint |
| 2026-05-12 | PPT Kanban action cards now mirror the Excel `buildActionCell` layout — bold action name, then per-checklist heading (`▸ <name>`) with each task as `  · <title>`, strike-through on completed | The previous "X/Y tasks" footer was useless when the user wanted to share a printable Kanban; richText segments + `fit: 'shrink'` keep the card readable even with long task lists |
| 2026-05-12 | Excel import modal: switch class names from `modal-*` to `v11-modal-*` so it picks up the existing centred-overlay CSS in `index.css`. Excel import also detects coloured cells without text as task signals via a new `isMeaningfulFill` filter (rejects transparent / white / pure black) — surfaces as `hasColorOnly` on each `monthSignal` | The bare `modal-*` classes had no matching CSS rules so the modal collapsed inline at the bottom of the page. The colour detection extends the import to roadmaps that highlight months instead of writing placeholder text |
| 2026-05-07 | Extend the `visibilitychange` listener to also run a Realtime catch-up on tab-visible: `fetchServerState(knownUpdatedAt)` then route any conflict through `processRealtimePayload` (or queue if guards active) | Chrome/Safari throttle WebSocket delivery on hidden tabs so Realtime updates emitted while tab B was hidden could take an arbitrarily long time to surface in tab B's UI. The 2-pass OCC fetch is ~50 bytes when nothing changed, so the catch-up is essentially free in steady state |
| 2026-05-04 | Reduce Supabase egress: `fetchServerState` now does a 2-pass OCC (timestamp first, full `board_data` only when conflict detected); `saveToSupabase` success path stops writing legacy `categories`/`actions`/`tasks` columns (legacy fallback still writes them when `board_data` column is missing); add `visibilitychange` listener that mirrors pending edits to localStorage when the tab is hidden as a safety net | Free-tier 5 GB egress quota was tight; the previous OCC fetched the full envelope on every save, which dominated egress. The legacy columns at the root duplicated data already inside `board_data` (~20-40 % per write). The visibilitychange handler covers the gap when a user backgrounds the tab during the debounce window |
| 2026-04-24 | Sync pushes `closed: false` to Trello when the local entity is fresher than the archived card — recovers from the "undo a delete, then sync re-archives the card locally" loop | `restoreSnapshot` gives the restored task/action a fresh `updatedAt`, but `if (card.closed)` in both sync paths used to unconditionally mirror Trello's archive state locally. By comparing `updatedAt > max(trelloLastModified, card.dateLastActivity)` we let local win and push an un-archive before falling through to the normal "local wins" branch |
| 2026-04-24 | Add two safety watchdogs in `App.jsx` — one force-resets `trelloSyncStatus` from `'syncing'` → `'idle'` after 45 s, another clears a stuck `isUserInteractingRef` after 60 s of continuous "interacting" | Users reported the Trello sync spinner staying on forever after certain sequences. Any code path that fails to clear the status (unhandled rejection, torn-down component, interrupted drag) is now recovered automatically instead of requiring a page reload |
| 2026-04-24 | PPT Timeline builds its grid via `pptx.addTable` and overlays native `roundRect` shapes only for task bars; PPT Kanban action cards now render the nested task list with strike-through + "+N more" overflow | User found the previous "rect-per-cell" rendering too noisy to edit. Using the native table primitive gives a cleaner, resizable skeleton and reduces shape count to 1 per task. Kanban action cards now mirror the Excel card-as-action layout |
| 2026-04-24 | DashboardView — KPIs "Progress by Category" groups by board in combined view; new "By Country" block aggregates on `task.countries[]` | Filed by the user as two separate asks; same view, so delivered together. By-country only renders countries with ≥ 1 tagged task to avoid a noisy 16-row default |
| 2026-04-24 | Combined view Kanban / Calendar / Timeline banner — dedicated legend + board-coloured 4 px vertical stripe on cards; timeline board name overflows the sticky label into the months zone to stay fully readable | Prior 6 px round dot on the card title was too close to the priority dot; ellipsis-truncated board name was unreadable for long names |
| 2026-04-24 | Fix three regressions from the v1 ship: `Icon.X` → `Icon.Close` (import crash), horizontal-noop threshold on Timeline drag (no schedule change when drag is almost purely vertical), `view='status'` alias added to the Kanban-export legend exclusion | Three small bugs identified from user feedback after the first deploy |
| 2026-04-24 | Rewrite the Excel import (`excelMapping.js` + `ExcelImportModal.jsx`) on a month-anchored grid algorithm and treat each workbook sheet as one board | The previous multi-level / super / flat-category / colour-signal heuristic produced wrong classifications on both reference files (Country Marketing Plan and MC Strategy Roadmap). New rule: any row below the month header with a label but no month content is a category, any row with month content is an action with one task per filled cell (numeric → budget task, text → titled task), horizontal merges span months, vertical merge fragments are ignored. Stricter month pattern (full-cell, ≤12 chars) fixes the "Mar matches Marketing" false-positive that corrupted the column map |
| 2026-04-24 | `handleExcelImport` accepts an array of boards (multi-board commit in a single setBoardData) in addition to the legacy single-board preview shape | The new modal exports one board per sheet; routing all of them through one state update keeps undo/redo coherent and avoids N intermediate renders |
| 2026-04-23 | Add PPT export (`src/lib/pptExport.js`) via `pptxgenjs` — native editable shapes, 16:9 widescreen, single slide for Timeline (12 months), auto-split Kanban at 6 category columns per slide | User wanted a presentable, editable slide format that mirrors the on-screen Timeline + Kanban. Rasterising was rejected — users need to edit titles and colours post-export. Dynamic import keeps the ~373 KB library out of the main bundle |
| 2026-04-23 | `restoreSnapshot` strips `_trelloBaseline` + `_inheritChannels` / `_inheritCountries` / `_inheritOtherLabels` on every restored entity whose `SYNC_FIELDS_*` diverged from current | The snapshot carries the PRE-edit baseline; after restore, the baseline matches the restored values → `buildSelective*Update` saw "no diff" → nothing was pushed → next sync let Trello win and silently reverted the undo. Clearing the baselines forces the "no baseline → full update" path in trelloSync.js so the restored state reaches Trello |
| 2026-04-23 | TimelineView drop detects lane collisions and pushes occupants down by +1 via `onBatchUpdateTasks`, instead of letting `calculateSwimLanes` silently slide the dropped task to the next free lane | Users expected a "swap" when dropping a task above an existing one. Explicit push-down preserves the intent while keeping calculateSwimLanes's auto-placement for cascading collisions |
| 2026-04-23 | Kanban Excel export omits the status legend for `category` + `action` (by-status) views | Columns already encode the grouping (category per column, status per column) — the separate legend was redundant noise. Month / Quarter / Country views keep it |
| 2026-04-23 | Timeline Excel export renders task titles non-bold | User readability preference — bold on every bar created visual heaviness in dense timelines |
| 2026-04-23 | TimelineBar overflow label requires ≥40px free space to the right before spilling; otherwise it's skipped and the native `title` tooltip is the only carrier for the full label | The label was bleeding into neighbour bars in dense lanes, creating false attribution. Ellipsized max-width + neighbour-distance guard removes the collision without hiding the information |
| 2026-04-23 | Combined view banners: Kanban group uses `flex-shrink: 0` + nowrap-ellipsis banner; Timeline board header switches to `white-space: nowrap` + `text-overflow: ellipsis` + full-name `title` | Previous `minWidth: 0` + `word-break: break-word` allowed the group to collapse below its columns' real width (each 280px fixed), causing the banner to overlap neighbour groups horizontally. Timeline banner was wrapping to 2 lines which broke the row-height grid alignment |
| 2026-04-22 | Add 10s recent-undo window in `useUndoRedo` (exposed via `recentUndoRef`) that blocks the Realtime handler, the pending-Realtime drain, the pre-save conflict fetch, and the GitHub polling merge so none of them can re-apply the pre-undo server state before the Trello push of the restored state completes | `restoreSnapshot`'s `updatedAt` bump was shown to be insufficient — a delayed Realtime echo or a pre-save conflict fetch would merge the old server state back into `boardDataRef` even though the task-level LWW would have eventually stamped the restored value. The window complements the existing `_saveId` echo filter for cases where the server's `updated_at` or the saveId has already rotated |
| 2026-04-22 | Flat-category grid import: rows whose own label sits next to distinct month-cell titles become a category + one task per month cell (vs the prior hierarchical "label on a row, tasks on sibling rows" layout) | User's Marketing/Employer Brand calendars use a single row per category with one task name in each month column. Previous heuristic tagged those rows as tasks and dropped the label. Distinct-text threshold (≥2) keeps the hierarchical "Campaign A \| x \| x \| x" pattern untouched |
| 2026-04-22 | Rows whose label was only injected by a vertical merge default to `ignore` instead of `category` | Phantom rows appeared between a UK-band header and its content; they were previously swallowed into empty categories. The new heuristic compares the row's own label column to the expanded one and routes merge-only labels to `ignore` |
| 2026-04-22 | Replace the "Apply to similar" button with a three-scope popover ("Apply to…▾") — same indent / same section only / same signal | Single scope was ambiguous and too broad. Explicit scopes + affected-row counts let users see exactly what each action propagates before clicking |
| 2026-04-22 | Status legend column rendered in Kanban + Timeline exports (right of the data area) + row height computed per row | Recipients didn't have a decoder for the status colors, and multi-line action cells were ellipsized by the fixed 34-px row height. Timeline unfreeze + dynamic row height now make both sheets print-ready |
| 2026-04-22 | Kanban card-as-action cells switch to exceljs richText so only the action name is bold — checklists/tasks stay regular weight; completed items get `font.strike` individually | Whole-cell bold lost the hierarchy. Per-segment richText preserves readability while keeping the single-cell summary layout |
| 2026-04-22 | swimLane is a preference, not a pin — `calculateSwimLanes` scans downward from the stored lane for the first free slot; `handleActionRowDrop` stores the raw cursor lane | Users wanted vertical control but never wanted two bars to overlap. Single-pass placement replaces the pinned/auto two-phase split so a drag just says "I'd prefer this row" and the algorithm guarantees no collision |
| 2026-04-22 | Kanban combined-view: group columns per source-board under a horizontal colored banner (vs prior vertical separator strip); board names are rendered in full, without leading bullet or "Board:" prefix | Vertical strip was hard to read and truncated long board names. Users asked for a classic section header above the column strip |
| 2026-04-22 | Kanban "By category" tab renames to "By Board" while combined view is active | Same data pivot, but labelled for what it actually represents when multiple boards are merged |
| 2026-04-22 | Timeline "Tasks/Actions" sidebar header always says "Actions" (was swapping to "Tasks" in card-as-task mode) | Users preferred the consistent label — they read the entries as Actions regardless of sync mode |
| 2026-04-22 | Timeline bar style: overflow-visible title span that spills to the right of bars narrower than 80px, dark text + white text-shadow halo for readability on both the colored bar and the timeline background | Ellipsizing a short bar's title hid the name entirely. The halo keeps the text legible on both surfaces without changing the bar's own styling |
| 2026-04-22 | Remove "Export Calendar (Excel)" menu entry from the app toolbar (function kept in the library) | Users preferred not to see it in the main export list; keeping the function allows a future menu entry to re-enable without a code change |
| 2026-04-22 | Propagate undo/redo/jumpTo back to Trello via `restoreSnapshot` (diff current vs snapshot, bump `updatedAt` on changed entities; queue lost `trelloCardId/trelloListId/trelloCheckItemId` in `trelloSync._pendingUndoDeletes` for `flushPendingUndoDeletes` at next sync) | Snapshots carried stale `updatedAt` — LWW saw them older than `card.trelloLastModified` and pulled from Trello, silently reverting the undo. Bumping `updatedAt` on the diff makes LWW push restored values; the deletes queue handles undos of creations so the orphan Trello card/list is archived instead of re-imported on the next pull |
| 2026-04-22 | Rewrite Timeline Excel export to mirror the grid import layout (1 label col A + 12 month cols B–M; task titles live inside the merged Gantt bar) | Previous export wrote titles in col C and used only a status glyph in the bar — the file didn't round-trip through the import parser and users got redundant text. New layout: category/action as full-width rows in A, tasks as Gantt bars carrying their own title, so exports feed directly back into the import wizard (supersedes 2026-04-21 Timeline export) |
| 2026-04-22 | Switch Kanban "By category" export to action-centric cells in card-as-action categories (cell = `action.name\n▸ checklist\n  · task`) | Card-as-action boards were flattened to per-task rows — stakeholders lost the action→checklist→task hierarchy and couldn't see which action owned which tasks. Per-category detection (`actions.some(a => !a.isDefault)`) switches rendering without a mode prop so card-as-task categories in mixed boards keep the per-task layout |
| 2026-04-22 | Add "By status" Kanban export view (one column per `CONFIG.STATUSES` entry in native order) | Users needed a status-oriented grouping for weekly standups that the existing 4 views didn't cover. Status view stays task-level regardless of sync mode because action-level status mixing would lose information |
| 2026-04-22 | Detect task cells by fill color in Excel import (`hasMonthSignal = hasMonthContent \|\| hasMonthColor`) via a parallel exceljs read of the workbook; widen "Apply to similar" to same-depth OR same-colIndex (exclude clicked row) | Many roadmaps flag tasks with a color highlight and no text — the old heuristic classified those rows as empty. parseWorkbook now dynamic-imports exceljs to extract ARGB fills alongside the xlsx data read; `isMeaningfulFill` filters transparent/white/black so blank-styled rows aren't false positives. "Apply to similar" relaxation (supersedes 2026-04-21) lets a single click propagate a manual override across all rows sharing the indentation, independent of month-content parity |
| 2026-04-22 | Drop the purple pin dot on Timeline bars (pinning stays functional) + let short-bar titles overflow visibly to the right (dark color + white text-shadow halo) | The pin dot was visual noise that reviewers confused with priority or status markers. Short-bar titles were ellipsized into invisibility — overflowing onto the white timeline background keeps the title readable at any bar width, and the halo textShadow maintains contrast on both the colored bar and the timeline background |
| 2026-04-21 | Group Kanban + Timeline views by source board in combined multi-board mode with colored banner headers + orange read-only banner above all views | Previously combined view merged all entities flat with only a 6px colored dot on cards — users couldn't quickly distinguish which board a column/action belonged to. `useMultiBoardData` now returns `boardGroups`; Kanban inserts a vertical colored separator between column groups from different boards, Timeline inserts a full-width colored banner row before each board's categories. Orange top banner confirms read-only status with "Exit combined view" button |
| 2026-04-21 | Remove Shift requirement for Timeline vertical pinning — any drag with ΔY>8px pins to lane (supersedes 2026-04-21 Shift+drop) | Shift+drop was poorly discoverable — users didn't know pinning existed. Now pure horizontal drag (ΔY≤8px) preserves current lane behavior, any vertical movement pins. Threshold prevents accidental pin from jittery mouse movements during horizontal resize |
| 2026-04-21 | Enhance Excel import: re-run `analyzeGridRows`+`autoAssignLevels` on sheet switch, add single-depth fallback (no indent → mois-content discriminant), widen "Apply to similar" match to same-depth OR same-colIndex with count preview | Users switching sheets from review step saw stale analysis from the previous sheet. Heuristic failed on flat tables with no indentation. "Apply to similar" only matched strict-equal indent, missing logically-equivalent rows in nearby columns — broadcast was confusing because users didn't know how many rows would be affected |
| 2026-04-21 | Timeline Excel export: remove task title from Gantt bar cells (keep title only in column C) | Previous export wrote task title in both column C (label) AND the merged Gantt bar cell — double text made bars unreadable in Excel since the label column already shows it. Bars now show only status color + optional 1-char status glyph (✓/▶/●/⏸/⏵/✕) when spanning ≥3 months |
| 2026-04-21 | Kanban Excel export: add `view` parameter (`category`/`month`/`quarter`/`country`) with submenu in App toolbar | Users needed different groupings for different stakeholders — month/quarter for execs, country for regional teams. `exportKanbanXlsx(tasks, categories, actions, view)` groups by the chosen axis; App toolbar now shows a mini-dropdown with 4 options instead of a fixed button |
| 2026-04-21 | Historique: add coalescing to `useUndoRedo.pushState` (skip new entry within 400ms if same label) + descriptive labels at all 16 call-sites + truncate HistoryPanel labels to 60 chars | Previous implementation created one history entry per pixel of drag/resize → panel polluted with hundreds of "Updated" entries. Coalescing collapses continuous gestures into a single entry while preserving the pre-change snapshot (not replaced). Descriptive labels (`Task 'Name' updated`, `Action 'Name' created`) make the panel actually useful for navigating history |
| 2026-04-21 | Switch Kanban/Timeline Excel exports to `exceljs` (calendar stays on `xlsx`); expose `buildKanbanWorkbook` / `buildTimelineWorkbook` as testable builders separate from the download wrappers | Old `xlsx`-only exports produced unreadable plain tables — stakeholders receiving these files had no visual cue for status/priority/category. `exceljs` supports `cell.fill`, `cell.font`, `cell.border`, and merges natively, giving us real Gantt bars (merged across the month range, filled with `statusColor`) and Kanban cards (thick status-colored left border on a light-gray card). Keeping calendar on `xlsx` avoids duplicating the month-per-sheet flow; `exceljs` ships as a lazy-loaded chunk (~271 KB gz) so users who never export pay nothing |
| 2026-04-21 | Split Excel grid parsing into `analyzeGridRows` / `autoAssignLevels` / `buildGridHierarchy` and add a manual review step in `ExcelImportModal` | Old `parseGrid` used a single heuristic ("label with no month content = category") that collapsed country bands (France/UK/Spain) and objective groups (Brand Awareness/Consideration) into flat categories, losing the hierarchy and the country tag — the new pipeline detects indent depth, wide-merge super-headers and country aliases (incl. FR/DE/IT/PT), auto-assigns a 4-level structure, and lets the user correct any row via a level dropdown with "Apply to similar" broadcast; non-country supers prefix child names ("Corporate - Marketing") while country supers propagate a `countries[]` tag down to tasks |
| 2026-04-21 | Wire multi-board combined view UI in BoardSelector (toggle + checkbox multi-select) and route `multiBoardData` through `categories`/`actions`/`tasks` derived state; add source-board colored dot on TaskCard | `useMultiBoardData` and `handleToggleMultiBoard` were already implemented but the views still read from `currentBoard` so the hook's output was dead data — users could not actually open a cross-board view. Combined view stays read-only via the existing `isReadOnly` branch, source dot is a 6px colored disc driven by the hook's deterministic color palette so users can visually trace each card back to its board |
| 2026-04-21 | Add Timeline vertical pinning via `task.swimLane` (Shift+drop pins, right-click resets, cross-action drop auto-resets) | Users previously had no control over vertical placement — every reorder/resize re-computed swimlanes from scratch, so anyone trying to group related tasks on the same row lost their layout on the next interaction. Pinning is a local-only field (not pushed to Trello), two-phase calculator places pinned tasks first and auto-fills the rest, collisions between pinned tasks are allowed since the user explicitly chose the lane |
| 2026-04-21 | Add Trello workspace picker to export wizard + bidirectional board rename sync via baseline | `createTrelloBoard` used to drop every export in Personal regardless of what workspace the user actually lived in; new step lets them pick an organization (or Personal) before creation. Board rename now syncs both ways using `trelloBoardNameBaseline` (last-synced Trello name) because Trello exposes no per-field timestamp — baseline comparison is the only reliable change-direction signal, first sync initializes without push/pull to avoid spurious renames |
| 2026-04-21 | Distinguish Trello 401 (token invalid → global logout) from 403/404 (no board access → switch to read-only) via `err.status` propagated from `trelloFetch`; track denied boards in a session-only `accessDeniedBoardIds` Set with orange banner + Unlink Trello action | `isReadOnly` previously collapsed every error into a generic sync failure — users kept retrying against a Trello board their account couldn't open. Storing the flag in component state (not on the board envelope) avoids pushing a user-specific permission failure to Supabase/GitHub where it would then block other legitimate collaborators |
| 2026-04-21 | Replace undo/redo arrow buttons with a single HistoryPanel side sheet; extend useUndoRedo with jumpTo/suspend/resume/getHistory and MAX_HISTORY 40→60 | User couldn't review past actions or skip several steps at once; two arrows consumed toolbar space without surfacing what the history contained — new panel shows all snapshots with timestamps and lets the user jump to any point. Trello sync calls `suspend()`/`resume()` so remote mutations don't pollute the user-facing history |
| 2026-04-21 | Reduce snapshot ring buffer from 3 slots to 1 + auto-clean legacy slots 1/2 on load | Three full-board snapshots saturated localStorage (QuotaExceededError on every auto-save), competing with the offline backup `marketing_tracker_backup` and the new `mkt_trello_oauth_token` OAuth fallback; `listSnapshots`/`restoreSnapshot` have no UI consumers so a single "last known good" slot is sufficient |
| 2026-04-21 | Extract Trello OAuth callback IIFE into external `public/trello-callback.js` (supersedes 2026-04-20) | `vercel.json` CSP `script-src 'self'` silently blocked the inline script; popup displayed "Authorization complete" but never executed the IIFE, so the three-channel delivery added the day before never ran — no token ever reached the main window |
| 2026-04-20 | Add BroadcastChannel + localStorage fallback to Trello OAuth callback IPC | trello.com sends COOP: same-origin, which severs `window.opener` even after popup redirects back to our origin; `postMessage` alone silently fails in modern browsers, leaving users stuck on the "Domain not registered" manual-paste screen despite correct Allowed Origins config |
| 2026-04-13 | Split comment fetching from board endpoint into client-side batches | Board fetch with 172+ cards timed out (35 sequential comment batches > 8s client + 10s Vercel limit); now board returns in ~2s, comments fetched separately in batches of 30 |
| 2026-04-13 | Increase Vercel maxDuration 10→60, client timeout 8s→30s | Safety margin for large boards; old 8s/10s was too tight even without comments |
| 2026-04-13 | Add conditional comment fetch (lastCardTimestamp) + dedup Set + action Map + parallel URL resolution | card-as-task sync was ~2.4x slower than card-as-action; conditional fetch skips unchanged cards, lookup maps reduce O(n) to O(1) |
| 2026-04-12 | Fix sync lock comment (kept 15s, corrected misleading Vercel reference) | Comment said "aligned with Vercel 10s timeout" but sync runs client-side; benefit of 30s was marginal |
| 2026-04-12 | Add 19 sync audit tests (syncAudit.test.js) | Prevent regressions on 8 baseline/merge bugs; cover multi-cycle, post-sync merge, multi-user realtime |
| 2026-04-12 | Fix 8 sync bugs: baseline refresh, null guard, comment attachments | Stale baselines caused re-push loops; null updates crashed API; comment attachments posted before upload |
| 2026-04-11 | Add loadCompleted flag + save validation + Realtime load guard + sessionStorage saveId persistence | Prevent data loss on deploy: auto-save fired before cloud data loaded; Realtime echoes from previous instance not detected after reload |
| 2026-04-11 | Setup TypeScript 6 progressive migration (types.ts, handlers.ts, useTouchDrag.ts, useFocusTrap.ts) + tsconfig.json | Type safety for core entities and hooks; allowJs:true + strict:false enables gradual migration without blocking build |
| 2026-04-11 | Add Kanban virtualization via @tanstack/react-virtual (VirtualKanbanCards.jsx, threshold 50) | Large Kanban columns (50+ cards) caused scroll lag and DOM bloat; virtual rendering keeps DOM light |
| 2026-04-11 | 14-step refactoring: ESLint, a11y CSS, code splitting (React.lazy), React.memo (11 components), useCallback (~20 handlers), Skeletons, context split (BoardContext+FilterContext), useFilters extraction, TimelineView decomposition, ActionDetailModal decomposition, CRUD handler tests (517 tests), focus trap + ARIA on 7 modals | Comprehensive code quality, performance, accessibility and maintainability audit |
| 2026-04-10 | Add _recentlyDeletedCardIds/_recentlyDeletedListIds to both sync modes | Async archive race condition: sync imports card/list as new entity before archiveTrelloCard completes |
| 2026-04-10 | Add _trelloBaseline + fix trelloLastModified timing in card-as-action new push | New actions/items lacked baseline for selective push; trelloLastModified set before checklist creation caused false conflict on next sync |
| 2026-04-10 | Update handleDeleteAction to track deleted card IDs via updateCurrentBoard | Card-as-action action deletion had same re-import race condition as card-as-task task deletion |
| 2026-04-07 | Add post-sync merge to preserve local edits during Trello sync | Sync replaced entire board, losing user edits (task creation, moves) made during the multi-second sync window |
| 2026-04-07 | Use buildSelectiveCheckItemUpdate for local-only checkItem push | mapTaskToCheckItemUpdate always sent state, causing "marked incomplete" Trello activity spam on every reorder |
| 2026-04-07 | Fix handleAddTask to use sibling trelloChecklistName in card-as-action | Hardcoded 'Tasks' created spurious checklists instead of adding items to the action's existing checklist |
| 2026-04-07 | Add orderUpdatedAt in handleUpdateTask when order changes | Position sync to Trello didn't detect order changes from single-task updates (e.g., cross-action moves in timeline) |
| 2026-04-03 | Revert to showArchived, delete→remove entities, fix checklist position sync | User expects archived items hidden by default; deleted cards should disappear entirely; position-only Trello changes were silently dropped (supersedes 2026-04-03 hideArchived) |
| 2026-04-01 | Fix archive reappearance: set board_data=null in legacy fallback + add visibleActions filtering | Stale board_data in Supabase caused Realtime to restore pre-archive state; archived actions (card-as-action) were never filtered from views |
| 2026-04-01 | Add legacy-column fallback in saveToSupabase when board_data column missing | Entire save failed if board_data migration wasn't run, causing "Cloud save failed" and loss of all sync results on refresh |
| 2026-03-31 | Fix post-sync auto-save: use syncRealtimeGuardRef (not isReceivingRealtimeRef) to block Realtime after sync | isReceivingRealtimeRef blocked auto-save, so synced data (archive/delete/positions) was never persisted — lost on page refresh |
| 2026-03-31 | Use buildSelective*Update for no-conflict local push paths (lines 847, 1558) | map*ToTrelloCardUpdate always sends dueComplete, triggering false "completed this card" activity on Trello for any field change |
| 2026-03-31 | Unlink entities from Trello on permanent card deletion (trelloCardId=undefined, trelloUnlinked=true) | Previous behavior just paused — but "push new tasks" would re-create a Trello card for the unlinked entity |
| 2026-03-31 | Add Realtime unsaved-edit guard (autoSaveTimeoutRef check), concurrent tab detection (BroadcastChannel), and _saveId echo filter | Realtime could overwrite pending edits during debounce window; concurrent tabs caused silent data loss; timer-based echo detection was unreliable |
| 2026-03-31 | Add save fallback cascading (Supabase → GitHub → localStorage) + validateBoardIntegrity on Realtime incoming data + localStorage quota guard | Supabase outage silently lost data; corrupted Realtime data propagated across clients; localStorage quota errors failed silently |
| 2026-03-30 | Fix card-as-action: pause action (not just tasks) when Trello card is deleted/archived; add unarchive restore logic | Action stayed `status: 'active'` when its card was deleted/archived on Trello — only tasks were paused |
| 2026-03-30 | Fix ghost tag: move `trelloLastModified` after all push operations + preserve local labels in "neither changed" path | Push API calls update `dateLastActivity` on Trello; setting `trelloLastModified` before caused false `trelloCardModified` on next sync, pulling stale labels |
| 2026-03-30 | Fix duplicate list: dedup by name before `createTrelloList` + use `activeListsCA` in pull phase (both modes) | Creating a local category matching an existing Trello list created a duplicate; pull phase iterated all lists including archived |
| 2026-03-27 | Fix label mapping persistence: save `mappingConfig.labelMappings` to `syncedBoard.trelloSync.labelMappings` + update `_inherit*` baseline after push | New labels created by pushActionLabelsToTrello/pushTaskLabelsToTrello were lost across sync cycles, causing tags to disappear after subsequent modifications |
| 2026-03-26 | Fix card-as-action: `mergeCheckItemIntoTask` now syncs `trelloChecklistId`/`trelloChecklistName` from Trello parent checklist on pull | Items moved between checklists on Trello kept stale group membership locally; only update in pull paths to avoid overwriting local move intent |
| 2026-03-26 | Fix card-as-action: remove tasks when Trello checklist item/checklist is deleted; add null guard at line 1596 for multi-action sync | Item deletion set `trelloItemDeleted` flag but never removed the task; checklist deletion set task to null but crashed on next action's loop accessing `null.actionId` |
| 2026-03-25 | Add selective push with `_trelloBaseline` for all content fields in both modes | Full push overwrote Trello changes to fields the user didn't touch (e.g., changing name locally overwrote Trello's date change); now only locally-modified fields are pushed, non-pushed fields are merged from Trello |
| 2026-03-25 | Extend label sync decoupling to card-as-task mode with `_inherit*` fields + label guard | Same label re-addition bug as card-as-action also affected card-as-task; also fix `mergeTrelloExtrasIntoTask` union merge overwriting pushed labels |
| 2026-03-25 | Fix card-as-action label sync: decouple label sync from content LWW — use `_inheritChannels`/`_inheritCountries`/`_inheritOtherLabels` baseline to detect local label changes; pull Trello labels when user only changed content (supersedes 2026-03-24) | When both sides changed and local won content, stale labels were re-pushed to Trello because `{ ...action }` preserved old tags; now labels from Trello are merged when user didn't explicitly change labels |
| 2026-03-24 | Fix card-as-action label removal sync: pass merged action (not original) to `pushActionExtrasToTrello`, remove `...action` spread that overwrote pulled labels | Removing a label on Trello was reverted by sync because extras push spread the original action (with old tags) over the merged action, then label push re-added them |
| 2026-03-23 | Fix card-as-action sync: position push guard uses `orderUpdatedAt` (not `actionHadLocalPush`), add cross-checklist item move via `idChecklist`, remove tasks when entire checklist deleted on Trello | Content-only push was overwriting Trello reorders; moving tasks between groups created duplicate checklists; checklist deletion on Trello left zombie tasks locally |
| 2026-03-23 | Add task/group deletion in ActionDetailModal (card-as-action): `handleDeleteTaskGroup` handler + delete buttons with confirmation popups | Tasks and task groups had no delete UI in the action modal — users could only delete the entire action |
| 2026-03-23 | Fix card-as-action checklist reorder sync: use composite order (`checklist.pos * 65536 + item.pos`) in `mergeCheckItemIntoTask` | Plain `item.pos` ignored checklist-level position changes — reordering checklists on Trello had no effect on task group order, and App→Trello reorders reverted on next pull |
| 2026-03-22 | Fix Trello polling interval reset: use ref pattern for handleTrelloSync in polling useEffect; clean listToCatId after category removal; add updatedAt to mergeCardIntoAction | Polling useEffect depended on handleTrelloSync callback which changed on every board/status update — interval timer reset constantly so auto-sync never fired; stale listToCatId entries caused cards to map to removed categories |
| 2026-03-22 | Fix card-as-action position push feedback loop: update trelloLastModified on tasks/action after position push | Position push updated card.dateLastActivity on Trello without updating local trelloLastModified, causing next sync to falsely detect Trello changes and overwrite local order values — groups jumped randomly |
| 2026-03-22 | Fix card-as-task checklist/item position pull in mergeTrelloExtrasIntoTask (add order field + sort by Trello pos); fix KanbanView filter/search for card-as-action mode (filter actions by matching tasks, pass filteredTasks to ActionCard) | mergeTrelloExtrasIntoTask was overwriting position sort by not capturing order from Trello pos; ActionCard received unfiltered tasks and action cards were never filtered out when no tasks matched search/filter criteria |
| 2026-03-22 | Fix mention regex to support Unicode accented chars (markdown.jsx + MentionInput.jsx); fix card-as-action checklist/item position pull + push guard (trelloMapping.js + trelloSync.js) | `\w` regex truncated accented member names in rendered mentions; `mergeCheckItemIntoTask` didn't pull `order` from `item.pos`, and position push always ran even when Trello won — overwriting Trello reorders |
| 2026-03-22 | Fix regression: skip trelloCardId dedup in card-as-action mode in validateBoardIntegrity, remove duplicate header search bar (keep FilterSidebar search only) | Previous commit's auto-repair dedup deleted all but one task per action in card-as-action mode because multiple tasks legitimately share the same trelloCardId (checklist items on one card); header search was redundant with FilterSidebar search |
| 2026-03-22 | Fresh-eyes sync audit v2: add isSyncInProgress() to Realtime gate, auto-repair in validateBoardIntegrity, default action deletion guard, card-as-action task/action move sync, updatedAt on auto-created default actions | 4-agent parallel audit found Realtime could overwrite sync results (missing gate check), orphaned data was logged but never repaired, default actions could be deleted breaking Kanban, and entity moves in card-as-action mode were never pushed to Trello |
| 2026-03-22 | Exhaustive sync audit fix: add updatedAt/month to mergeCardIntoTask, preserve unmapped local channels/countries, add updatedAt to handleAddTask/handleAddAction/handleReorderTask, add pushActionLabelsToTrello for card-as-action, add trelloItemDeleted flag, add dedup guard on card import | After multiple rounds of sync fixes, systematic audit revealed 9 remaining bugs: missing timestamps broke last-write-wins, month not recalculated on pull, local-only channels lost on merge, action labels never pushed in card-as-action mode, deleted checklist items re-created as duplicates, race condition on double-sync created duplicate tasks |
| 2026-03-22 | Fix checklist/item position sync (isPushWinner flag), fix missing channels in mergeCardIntoTask, add label re-pull in mergeTrelloExtrasIntoTask, add @mention autocomplete in comments, add Action→Task tag inheritance, add Trello icon in board title header | Checklist reorders on Trello were overwritten by local push; channels from Trello labels were never pulled; after push, Trello-added labels were lost; users couldn't tag teammates in comments; action tag updates didn't propagate to linked tasks; Trello-synced boards had no visual indicator in header |
| 2026-03-22 | Tier 3 audit: 43 Vitest tests, quick-add inline Kanban, Context expansion (categories/actions/tasks/filters), responsive mobile CSS, Supabase Storage attachments with base64 fallback, 8s AbortController timeout on trelloFetch | Tests prevent regressions, quick-add reduces friction, Context reduces 26+ props drilling, mobile UX was unusable, inline base64 bloated JSON, Trello requests hung indefinitely |
| 2026-03-22 | Tier 2 audit: ErrorBoundary, HTML escaping in markdownToHtml, persistent sync indicator, global search, "My tasks" filter, LATE badges, onboarding overlay, field-by-field Realtime merge, Realtime connection monitoring | Audit revealed XSS via innerHTML, no error recovery, ephemeral save feedback, no onboarding, shallow Realtime merge losing fields, no connection monitoring |
| 2026-03-22 | Tier 1 audit quick wins: memoize KanbanView getColumns/filteredTasks, reduce sync lock 60s→15s, env vars for Supabase, CSP headers, DnD cursor affordance, goOnline 2s delay | Comprehensive audit revealed perf waste (recalc every render), stale lock exceeding Vercel timeout, hardcoded credentials, missing security headers, invisible drag capability, and Realtime race on reconnect |
| 2026-03-21 | Comprehensive sync audit: fix card list-move detection, category name sync (card-as-task), createCard missing fields, offline guard, drag guard for Kanban, auto-save sync lock | Card moves between Trello lists left tasks in wrong category; category names never synced in card-as-task; new cards created without start/pos/members; offline sync could overwrite local edits; Kanban drag didn't block auto-save; auto-save could write mid-sync data |
| 2026-03-21 | Add bidirectional deletion sync for tasks/actions: archive Trello cards on local delete, delete checklist items on task delete (card-as-action) | Tasks/actions deleted locally left orphaned cards on Trello that were re-imported on next sync; archiving (not hard delete) keeps reversibility |
| 2026-03-21 | Add missing syncMode to card-as-task import, add bidirectional list deletion sync, fix Realtime merge to preserve all trelloSync fields | syncMode was never set causing "New action" button to show, list deletion was one-way only, Realtime merge could lose trelloSync fields when incoming was undefined |
| 2026-03-20 | Fix card-as-task "New Action" bug, add touch DnD to all draggable components, CSS responsive mobile | allDefault now handles empty categories in card-as-task mode, useTouchDrag hook + inline touch on ActionCard/CalendarView/ActionDetailModal/TaskDetailModal/KanbanView columns, modals responsive at 480px, touch targets 44px on coarse pointer |
| 2026-03-20 | Strip Trello metadata on board duplication, fix showArchived reset on board switch, fix Kanban cross-column card drag disappearance, add delete confirmations on task/action/category | Duplicated Trello boards no longer sync to same source, filters fully reset on switch, cross-column drag now uses single onUpdateTask instead of broken batch reorder, accidental deletes prevented |
| 2026-03-20 | Full audit: replace all Date.now() IDs with crypto.randomUUID(), fix handleBatchUpdateTasks date priority, add missing timestamps/gradient to inline entity creation, hide Action section in NewTaskModal for card-as-task | 30+ ID collisions risk eliminated, batch reorder month mismatch fixed, inline-created entities now sync-ready with timestamps, Action UI fully hidden in card-as-task mode |
| 2026-03-20 | Audit fix: protect statuses from Trello overwrite, sync startDate, auto-sync after save, robust Realtime guard, add gradient/timestamps to new categories, hide Action dropdown in card-as-task | Comprehensive sync audit — TRELLO_PROTECTED_STATUSES prevents status reversal, post-save debounced sync replaces manual clicks, syncRealtimeGuardRef ties guard to save cycle, missing fields on new Trello categories caused visual bugs |
| 2026-03-20 | Fix month/quarter column assignment to use dueDate first, add list sync to card-as-task, auto-create default actions, hide all action creation UI, fix checklist reorder DnD | Multiple card-as-task bugs: month used startDate, new lists didn't sync, new categories had no default action, inline "Create action" was still visible, checklist drag events bubbled incorrectly |
| 2026-03-20 | Fix "Add" button in category view to create task (not action) for card-as-task boards + hide "New Action" in header | Creating actions in card-as-task mode breaks `allDefault` invariant — "Add" now opens NewTaskModal with default action pre-filled |
| 2026-03-20 | Remove "Action" label mapping option for card-as-task sync mode | Mixing default + non-default actions in one category breaks Kanban `allDefault` check — users wanting action-level organization should use card-as-action mode instead |
| 2026-03-20 | Fix Kanban card drag for directTasks category view + add Trello icon in board selector | card-as-task boards showed TaskCards but drop handler only checked `actionId`; inline batch reorder excluded category view |
| 2026-03-20 | Move getTaskMonth out of getColumns() to component scope | Root cause of persistent month/quarter reorder failure — function was unreachable from inline handler, causing silent ReferenceError |
| 2026-03-20 | Add handleBatchUpdateTasks for all multi-task reorder operations | N separate onUpdateTask calls caused React state batching to lose intermediate order updates |
| 2026-03-20 | Category column reorder now updates category.order field | Column drag only saved to localStorage, Trello sync couldn't detect position changes |
| 2026-03-20 | Add dedup guard in card-as-action checklist item pull | Tasks that lost trelloCheckItemId were recreated as duplicates on next sync |
| 2026-03-18 | crypto.randomUUID for all entity IDs | Eliminates ID collision risk from `Date.now()+Math.random()` |
| 2026-03-18 | CORS restriction via ALLOWED_ORIGIN | Replace wildcard `*` with env var on `api/trello.js` and `api/github.js` |
| 2026-03-18 | Remove sendBeacon dead code | `/api/save-beacon` endpoint never existed — localStorage sync is sufficient |
| 2026-03-18 | Offline mode with yellow banner | `navigator.onLine` detection; saves to localStorage only when offline, auto-resync on reconnect |
| 2026-03-18 | Snapshot ring buffer (3 slots) | Rotating snapshots `mkt_snapshot_0/1/2` on auto-save, 48h TTL, restorable via `restoreSnapshot(index)` |
| 2026-03-18 | Post-sync Supabase refresh (4s) | Light load after Trello sync recovers Realtime events ignored during sync lock |
| 2026-03-18 | Post-sync integrity check | `validateBoardIntegrity()` after every sync — orphan refs, duplicate IDs, missing syncMode |
| 2026-03-18 | Detailed sync error tracking | `result.errorDetails [{name, op, error}]` shows failed item names in notification |
| 2026-03-18 | Trello retry with exponential backoff | `trelloFetch` retries 3× on 429/502–504/network errors; backoff 1s, 2s, 4s |
| 2026-03-18 | Pre-sync snapshot (24h validity) | Save board to `localStorage('trello_sync_snapshot')` before sync; auto-restore on failure |
| 2026-03-18 | Sync lock with 60s auto-timeout | Prevents permanent sync blockage when Trello API hangs |
| 2026-03-18 | Checklist deletion guard | Only delete Trello checklists when `localChecklistIds.size > 0` — prevents wiping card-as-action data |
| 2026-03-18 | Card-as-action task guard | Skip tasks with `trelloCheckItemId`/`trelloChecklistName` in card-as-task path — prevents data corruption if syncMode lost |
| 2026-03-18 | Sync merge-by-ID for comments/checklists | Match by Trello ID (not name) — preserves local-only items, avoids duplicates |
| 2026-03-18 | Card-as-Action sync mode | Per-board `trelloSync.syncMode`: `card-as-task` (default) or `card-as-action` (Cards→Actions, ChecklistItems→Tasks) |
| 2026-03-18 | Realtime syncMode protection | Merge incoming Realtime data preserving local `trelloSync.syncMode` if incoming is missing it |
| 2026-03-18 | Checklist position sync via `pos` | Checklist and item order synced with Trello `pos` field; `Promise.all` for parallel updates |
| 2026-03-18 | Member picker via ReactDOM.createPortal | Escapes modal stacking context — prevents picker clipping |
| 2026-03-18 | Comment deduplication before push | Check text match before pushing to Trello — prevents duplicate comments |
| 2026-03-18 | OAuth via postMessage | Replace `callback_method=fragment` — no return_url whitelist needed |
| 2026-03-18 | Guest read-only on Trello boards | Full read-only mode for guest users visiting Trello-linked board |
| 2026-03-18 | Auth gate (AuthGate.jsx) | Trello OAuth login or guest password; guest uses sessionStorage (expires on tab close) |
| 2026-03-18 | Enhanced Markdown (SimpleMarkdown) | Headings, blockquotes, fenced code blocks, ordered lists, strikethrough, hr — no innerHTML |
| 2026-03-18 | WYSIWYG contentEditable descriptions | Replace textarea with contentEditable + markdown conversion in TaskDetailModal/comments |
| 2026-03-18 | Unified task creation modal | All entry points (Kanban/Timeline/Calendar/header) use `NewTaskModal` with `initialValues` prop |
| 2026-03-18 | beforeunload save flush | Flush pending auto-save debounce on tab close; synchronous localStorage save as guaranteed fallback |
| 2026-03-18 | Trello archived cards support | `filter=all` fetch; `card.closed` → `trelloArchived=true` + `status='paused'`; "Show archived" filter toggle |
| 2026-03-18 | Named checklists (task.checklists) | Replace flat `task.checklist`; named groups with per-checklist progress; `normalizeTaskChecklists()` migration |
| 2026-03-18 | Trello OAuth login per-user token | `X-Trello-Token` header sent per-user; server prefers it over env `TRELLO_TOKEN` |
| 2026-03-18 | Label remap after import | `TrelloImportModal` supports `mappingOnly` mode; "Re-configure Labels" in BoardSettingsModal |
| 2026-03-18 | Trello sync polling lifecycle | Managed in App.jsx via `trelloSyncIntervalRef`; starts/stops on board change or settings change |
| 2026-03-18 | Trello integration (Phase 2) | `api/trello.js` serverless proxy, import wizard, bidirectional sync, last-write-wins |
| 2026-03-18 | robots.txt + noindex meta | Block search engine crawling of the app |
| 2026-03-18 | Calendar +N more expands row | "+N more" expands week row in month view (click again to collapse) — no new task creation |
| 2026-03-18 | Calendar hover add button | ClickUp-style "+" hover button (bottom-left of day cell) replaces click-on-day task creation |
| 2026-03-18 | Calendar bar color system | All bars use category color gradient — consistent across single-day and multi-day tasks |
| 2026-03-18 | Calendar view added | Month/week modes, shared `computeWeekBars()`, drag-to-reschedule, ClickUp-inspired |
| 2026-03-18 | Draggable Kanban columns | Category and country views; order persisted to localStorage; `stopPropagation` separates card vs column drag |
| 2026-03-18 | Multi-board v2 format (Phase 1) | Boards array in data envelope; BoardSelector; BoardSettingsModal; `board_data` JSONB column in Supabase |
| 2026-03-18 | Inline category/action creation | NewActionModal creates categories inline; NewTaskModal creates actions inline |
| 2026-03-18 | Kanban "By Country" view | 5th grouping mode; all 16 countries as columns + "Unassigned"; drag replaces country (not appends) |
| 2026-03-18 | Vite migration (Phase 0) | Monolith `index.html` (~3800 lines) → Vite 5 + modular React components in `src/` |
| 2026-03-18 | CLAUDE.md created | Persistent memory file across Claude Code sessions |
| 2026-03-18 | isReceivingRealtimeRef flag | Prevent infinite loop between Supabase Realtime and auto-save |
| 2026-03-18 | Refs for save closures | Fix stale closure in save callbacks; `boardDataRef` updated synchronously before each save |
| 2026-03-18 | GitHub org migration | Moved `FbnCrr/Dashboard-marketing` → `migso-pcubed-mkt-com/Marketing-Dashboard` |
| 2026-03-18 | Dark mode removed (V3.0) | V11 design system migration — single-theme CSS only. Do NOT re-add `dark:` classes. |
| 2026-03-18 | Timeline swim lane freezing | Prevent visual jumps during drag/resize — freeze lane order during operations |
| 2026-02-28 | UI translated to English | International team accessibility |
| 2026-02-28 | Vercel serverless for GitHub proxy | Keep GITHUB_TOKEN server-side |
| 2026-02-28 | GitHub API as fallback storage | Original storage layer kept for resilience alongside Supabase |
| 2026-02-28 | Supabase as primary storage | Real-time sync needed for multi-device use |
| 2026-02-28 | Single index.html architecture (superseded 2026-03 Vite) | Initial simplicity choice — no build step, CDN deps. Superseded by Vite migration. |
